# M6 manual world backup v1

M6 is the sole writer of verified physical objects, object references,
`usedBytes`, `overageBytes`, and storage billing cursors. It consumes the
shared v1 policy, authorization, and canonical usage contracts. Public calls
require an XMCL account session; restore worker callbacks require a separately
authenticated service credential.
